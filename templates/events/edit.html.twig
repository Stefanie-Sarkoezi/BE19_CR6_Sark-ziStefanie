{% extends 'base.html.twig' %}

{% block title %}Edit Events{% endblock %}

{% block body %}
    <h1 class="text-center my-5 text-white fs-1">Edit Events</h1>

<div class="text-white mx-5 my-5">
    <div class="mt-5">{{ include('events/_form.html.twig', {'button_label': 'Update'}) }}</div>
    <div class="my-3">{{ include('events/_delete_form.html.twig') }} </div>
    <a href="{{ path('app_events_index') }}" class="btn text-white btn-dark">Back to List</a> 
</div>
{% endblock %}
